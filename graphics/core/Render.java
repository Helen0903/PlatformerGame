import java.awt.Graphics;

public abstract class Render {
    /**
     * The abstract function in which the programmer decides what is to be done on each frame.
     */
    public abstract void draw(Graphics g);
}